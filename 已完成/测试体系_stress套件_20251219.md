# 测试体系：Stress 套件（200 并发任务）与任务发布器并发安全（已完成）

## 新增内容
- `tests/test_stress_task_executor.py`
  - 并发发布 `200` 个任务（ThreadPoolExecutor）
  - 使用 `TaskExecutor` + `NoopHandler` 顺序消费任务
  - 校验 `task_id` 唯一、任务均完成、结果回写正常
- `tests/run_tests.py`
  - 新增 `stress` suite：`python tests/run_tests.py stress`
- `src/core/cloud/enhanced_publisher.py`
  - `EnhancedCloudTaskPublisher` 增加进程内 `RLock`，保证 `publish/update/list/get_ready/get_stats` 在并发调用下不会写坏 JSON 文件

## 运行方式
```bash
python tests/run_tests.py stress
```

