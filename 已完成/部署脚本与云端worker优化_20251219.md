# 部署脚本与云端 worker 可运行性优化（已完成）

## 变更点
- `scripts/setup.sh`
  - 增加可选开关：Node/pm2/Docker/Nginx/redis-server
  - 默认不强行安装 redis-server（更贴合 Docker Redis 场景）
- `scripts/deploy.sh`
  - 增加 `DEPLOY_MODE`：`pipeline_worker|inspector|mcp_server|all`
  - 增加 `RUN_TESTS`：`none|unit|integration|all`
  - 默认以 pm2 守护云端 `pipeline_worker`（更贴合“云端工具对接”场景）
- `src/core/cloud/pipeline_worker.py`
  - 增加 `src/` 兜底 `sys.path` 初始化，可直接脚本运行：`python src/core/cloud/pipeline_worker.py`
- `DEPLOY_GUIDE.md`
  - 同步脚本用法与常用参数示例

