# 配置指南

Heablcoin 使用 `.env` 管理所有敏感和可调参数。请始终基于 `.env.example` 复制生成 `.env`，并确保该文件不进入版本控制。

---

## 快速上手

1. 复制模板：`cp .env.example .env`
2. 填写必填项：交易所密钥、`USE_TESTNET`、邮件账户
3. 安装依赖：`pip install -r requirements.txt`
4. 验证：`python tests/run_tests.py unit`

---

## 核心配置项

### 交易所
- `BINANCE_API_KEY` / `BINANCE_SECRET_KEY`：Binance API 密钥
- `USE_TESTNET=True`：强烈建议先使用测试网

### AI 模型
- 可不填任何 Key（会回退离线 echo）
- 若要启用多模型，至少配置一个 provider 的 API Key

### 邮件通知
- `EMAIL_NOTIFICATIONS_ENABLED=True`：启用邮件通知
- 需配置 SMTP 服务器和凭证

### 风控限制
- `MAX_TRADE_AMOUNT`：单笔最大交易金额
- `DAILY_TRADE_LIMIT`：每日交易限额
- `ALLOWED_SYMBOLS`：允许交易的币种白名单

---

## 完整配置清单

完整变量解释与示例配置见项目根目录：[env配置说明.md](../../env配置说明.md)

---

> 📚 [返回文档索引](../文档索引.md)
