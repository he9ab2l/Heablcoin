# 配置指南 ⚙️

Heablcoin 使用 `.env` 管理所有敏感和可调参数。请始终基于 `./.env.example` 复制生成 `.env`，并确保该文件不进入版本控制。

## 快速上手
1) 复制模板：`cp .env.example .env`  
2) 填写必填项：交易所密钥、`USE_TESTNET`、邮件账户、至少一个 AI 提供商密钥（可选，未填则回退离线 echo）。  
3) 安装依赖：`pip install -r requirements.txt`  
4) 验证：`python tests/run_tests.py unit`（邮件链路可用 `python tests/run_tests.py email`）。

## 核心要点（不看全量清单也能跑起来）

- 交易所：填写 `BINANCE_API_KEY` / `BINANCE_SECRET_KEY`，并建议保持 `USE_TESTNET=True`。
- AI：可不填任何 Key（会回退离线 echo），但若要启用多模型，至少配置一个 provider 的 `*_API_KEY`。
- 邮件：只有在你需要通知/报告时才启用（`EMAIL_NOTIFICATIONS_ENABLED=True`）。
- 风控：建议至少设置 `MAX_TRADE_AMOUNT`、`DAILY_TRADE_LIMIT`、`ALLOWED_SYMBOLS`。

## 统一配置清单（权威入口）

完整变量解释与示例配置见：`env配置说明.md`。

路径：`../../env配置说明.md`
